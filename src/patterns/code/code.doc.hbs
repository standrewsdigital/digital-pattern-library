---
title: Code
---
<div class="row">
	<div class="col-md-8">
<p class="lead">The code pattern is used to display example source code on pages. For example, this may be used in documentation such as the digital pattern library or in blog post tutorials.</p>
<div class="alert alert--info">
<h2 class="alert__heading">Inline code</h2>
<p>Inline code may simply be wrapped with <code>&lt;code&gt;&lt;/code&gt;</code> tags.</p>
</div>
<h2>Rules for code</h2>
<ul>
    <li>The digital pattern library automatically encodes source code, so you may simply drop in the raw code between the <code>&lt;code&gt;</code> elements.</li>
    <li>The first line of your code must be on the same line as the opening <code>&lt;pre&gt;&lt;code&gt;</code> elements. This is because <code>&lt;pre&gt;</code> includes all whitespace and line-breaks within the tags. The following lines of code should be indented beginning from column 1.</li>
    <li>Along with 'pattern-source' a language class must used to match the required language. If a class is not included then the script will attempt to identify the language automatically. Common language classes include <code>apache</code>, <code>css</code>, <code>handlebars</code>, <code>html</code>, <code>javascript</code>, <code>json</code>, <code>markdown</code>, <code>php</code>, <code>sql</code>, and <code>xml</code>; for a full list of the 160+ supported languages see <a href="https://highlightjs.org/static/demo/">highlight.js</a>.</li>
</ul>

<h2>Options available</h2>
<ul>
{{#code-options.options}}
    <li><a href="#{{option-anchor}}">{{option-heading}}</a></li>
{{/code-options.options}}
</ul>
</div>
</div>
{{#code-options.options}}
<hr>
<div class="row">
	<div class="col-md-8">
<h3 id="{{option-anchor}}">{{option-heading}}</h3>
{{{option-description}}}
</div>
</div>
<!--noindex-->
	{{> code}}
<!--endnoindex-->
{{/code-options.options}}

<hr>
<h3>More examples</h3>
<p>For more language examples see the <a href="https://highlightjs.org/static/demo/">highlight.js demo</a>.</p>