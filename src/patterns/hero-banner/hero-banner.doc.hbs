---
title: Hero Banner
pattern: hero-banner
examples:
- name: hero-banner1
related:
---

<p>Hero banner displays a large image, a heading, some intro text and a call to action.</p>


<h2>Rules</h2>

<ul>
	<li>...</li>
</ul>

<h2>Syntax</h2>

<div class="pattern-source">{{> hero-banner}}</div>

<h2>Options</h2>

<ul>
	<li>By default there is no background image, you'll need to supply this by attaching inline style on <code>&lt;div class="hero-banner"&gt;</code>. For example:
		<code>&lt;div class="hero-banner" style="background-image: url(path/to/image.jpg)"&gt;</code>. The image will be be set to <code>cover</code> the background, so select a large image. The example image is <code>1905 x 363</code>.
	</li>
</ul>
