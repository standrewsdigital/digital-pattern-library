---
title: Contact card
---

<p>The contact card pattern is used to display information for staff, students and experts.</p>

<h3>Rules for contact cards</h3>
<ul>
	<li>This pattern must only be used on people pages in the main body or within the experts guide on the news site.</li>
	<li>Cards with an image must be at the size: 111 &times; 127 pixels.</li>
	<li>If an image is not available, use the default staff profile image: <a href='../../assets/core/images/contact-card-default.gif'>contact-card-default.gif</a></li>
</ul>
<h3>Options available</h3>
<ul>
{{#contact-card-options.options}}
    <li><a href="#{{option-anchor}}">{{option-heading}}</a></li>
{{/contact-card-options.options}}
</ul>

{{#contact-card-options.options}}
<hr>
<h3 id="{{option-anchor}}">{{option-heading}}</h3>
{{{option-description}}}
{{> contact-card}}
<span class="label label-default">Code</span>
<pre><code class="pattern-source html">{{> contact-card}}</code></pre>
{{/contact-card-options.options}}