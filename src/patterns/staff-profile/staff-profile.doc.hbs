---
title: Staff profile
---

<p>The staff profile pattern is used to display a list of staff. This pattern includes a short biography excerpt, contact details, and an optional link to each staff member’s full biography page.</p>


<h3>Rules for staff profiles</h3>
<ul>
    <li>Must only be used in the main body of the page.</li>
	<li>The biography excerpt must not exceed 550 characters.</li>
	<li>If a biography excerpt exceeds 550 characters, and there is a user case to include all information, then a standalone biography page should be considered as an alternative.</li>
	<li>The profile image must be 200 &times; 200 pixels.</li>
	<li>An image must be present if using a link to a full biography.</li>
	<li>The heading link and the full biography link must link to the same page.</li>
	<li>Contact information should be present if available.</li>
</ul>

<h3>Options available</h3>
<ul>
{{#staff-profile-options.options}}
    <li><a href="#{{option-anchor}}">{{option-heading}}</a></li>
{{/staff-profile-options.options}}
</ul>

{{#staff-profile-options.options}}
<hr>
<h3 id="{{option-anchor}}">{{option-heading}}</h3>
{{{option-description}}}
<!--noindex-->
	<span class="label label-default">Example</span>
</div>
<div class="pattern-wrapper">
	<div class="container">
{{> staff-profile}}
</div>
</div>
<div class="container">
<span class="label label-default">Code</span>
<pre><code class="pattern-source html">{{> staff-profile}}</code></pre>
<!--endnoindex-->
{{/staff-profile-options.options}}